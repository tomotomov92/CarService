@model CarService.Models.EmployeeRoleModel

@{
    ViewData["Title"] = "Edit Employee Role";
}

<h1>Edit Employee Role</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmployeeRoleName" class="control-label"></label>
                <input asp-for="EmployeeRoleName" class="form-control" />
                <span asp-validation-for="EmployeeRoleName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
